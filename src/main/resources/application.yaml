server:
  port: 8080
  servlet:
    context-path: /permitteringsskjema-api
info:
  app:
    version: '@project.version@'
    build:
      time: '@timestamp@'
management:
  endpoints:
    web:
      exposure:
        include: info, health, metrics, prometheus
      base-path: /internal/actuator
  endpoint:
    health:
      show-details: never
spring:
  jackson:
    default-property-inclusion: non-absent

permittering:
  arbeidsgiver:
    enabled: true
    topic: aapen-permittering-arbeidsgiver
  database:
    url: "jdbc:postgresql://${NAIS_DATABASE_PERMITTERINGSSKJEMA_API_PERMITTERINGSSKJEMA_API_DB_HOST}:${NAIS_DATABASE_PERMITTERINGSSKJEMA_API_PERMITTERINGSSKJEMA_API_DB_PORT}/${NAIS_DATABASE_PERMITTERINGSSKJEMA_API_PERMITTERINGSSKJEMA_API_DB_DATABASE}"
    username: ${NAIS_DATABASE_PERMITTERINGSSKJEMA_API_PERMITTERINGSSKJEMA_API_DB_USERNAME}
    password: ${NAIS_DATABASE_PERMITTERINGSSKJEMA_API_PERMITTERINGSSKJEMA_API_DB_PASSWORD}

kafka:
  topic: permittering-og-nedbemanning.aapen-permittering-arbeidsgiver
  keystore.path: ${KAFKA_KEYSTORE_PATH:/dev/zero}
  truststore.path: ${KAFKA_TRUSTSTORE_PATH:/dev/zero}
  credstore.password: ${KAFKA_CREDSTORE_PASSWORD:pwd}
  security.protocol: SSL
  brokers: ${KAFKA_BROKERS:localhost:9092}
